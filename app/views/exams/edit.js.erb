$('div#question-chooser').html('<%= escape_javascript render("include_question") %>');
$('div#questions-paginator').html('<%= escape_javascript(paginate(@questions, :remote => true, :window => 2, :outer_window => 0).to_s) %>');
$.fn.onReady();